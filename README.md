# where_my_money

Python package template for tracking and analyzing personal spending.

## Quick start

```bash
python -m venv .venv
source .venv/bin/activate
pip install -U pip
pip install -e .
pytest -q
```
